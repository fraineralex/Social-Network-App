<div class="grid-container">

    <div class="grid-item0"> 
        <div class="">
            <a href="#allFriends" class="SeeAllYourFriend">See All you friend</a>
        </div>
        <div class="">
            <a href="/searchNewFriendHome/{{userId}}" class="searchNewFriend">add new friend</a>
        </div>
    </div>
    
    <div class="grid-item1">
        <div class="box-center">
            {{#each postF as |p key|}}
            {{!-- post --}}
            <div class="post-box" aria-live="assertive" aria-atomic="true">
                <div class="box-header border-dark">
                    <span class="d-image">
                        <img src="{{p.author.dataValues.imageProfile}}" class="image-post" alt="{{p.author.dataValues.name}}">
                    </span>
                    <small class="me-auto fs-6"><strong>{{p.author.dataValues.name}}</strong></small>
                    <small class="float-end fw-light text-muted">{{getDate p.createdAt}}</small>
                </div>
                <div class="box-body">
                    <small class="fs-6 mt-1"></small>
                    <div class="">
                        <p>{{p.content}}</p>
                        {{#if (getConfirmationF ../imgConfirmation key)}}
                            <img src="{{p.src}}" alt="image {{p.author.dataValues.name}}">
                        {{/if}}
                    </div>
                </div>
                <div class="box-footer">
                    <a type="button" class="btn btn-primary float-end" href="/new-comment/{{p.id}}">
                        Comentar
                    </a>
                </div>
            </div>
            {{/each}}
        </div>
    </div>

    
   <div class="grid-item2" id="allFriends">
        <h1>Your Friends</h1>
        <hr>
        <div class="friendList">
            <ul class="">
                {{#each userF as |uf key|}}
                <li>
                    <div class="imgNameButton">
                        <a href="">    
                            <div class="imgName">
                                    <img src="{{uf.imageProfile}}" alt="profile {{uf.name}}" class="imgFriendList"> 
                                    <p>
                                        {{uf.name}} {{uf.lastName}}
                                    </p>
                            </div>
                        </a>
                        <div class="">
                            <button type="button" class="btn btn-primary" data-toggle="modal" data-target="#deleteF{{uf.id}}">
                                <i class="bi bi-trash"></i>
                            </button>
                        </div>
                    </div>      
                </li>

                <div class="modal fade" id="deleteF{{uf.id}}" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
                <div class="modal-dialog modal-dialog-centered" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="btn close btn-danger" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            <p>Estas seguro de que quieres borrar a <i>{{uf.name}} {{uf.lastName}}</i> de tu lista de amigos...?</p>
                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                                <a href="/delete/friend/{{../userId}}/{{uf.id}}" class="btn btn-danger">Delete</a>
                        </div>
                    </div>
                </div>
                </div> 

                {{/each}}
            </ul>
        </div>    
        <div class="">
            <a href="/searchNewFriendHome/{{userId}}" class="searchNewFriend">add new friend</a>
        </div>
    </div>
</div>